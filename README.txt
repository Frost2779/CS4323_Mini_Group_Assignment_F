-----Compiling-----
To compile this project it must be done on linux, as it is reliant of linux specific syscalls.

---Make---
To compile the project, simply run make with the following command:
make

---Manual---
If for some reason that make is not working, you can compile it manually with the following command:
gcc ./*.c -o mini_project